name: learn_flutter_animated_widget
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  avatar_glow: ^3.0.1
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  get: 4.6.6

dev_dependencies:
  flutter_lints: 4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/profileimage.png
